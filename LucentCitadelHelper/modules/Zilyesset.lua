LCH = LCH or {}
local LCH = LCH
LCH.Zilyesset = {}

--function LCH.Zilyesset.RyelazMeteor(result, targetType, targetUnitId, hitValue, abilityId)
--    if result == ACTION_RESULT_BEGIN then
--      --LCH.Alert("Wamasu", string.format("Charge -> %s", LCH.GetNameForId(targetUnitId)), 0xFFD666FF, abilityId, SOUNDS.OBJECTIVE_DISCOVERED, hitValue)
--      CombatAlerts.AlertCast(abilityId, "", hitValue, {-2, 1})
--    end
--end

function LCH.Zilyesset.Init()

end

function LCH.Zilyesset.UpdateTick(timeSec)

end
